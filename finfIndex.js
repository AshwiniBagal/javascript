function indexIfArr(arr , num){

    let char;
    for (let index = 0; index < arr.length; index++) {
          char = arr.indexOf(num);
        
    }
    console.log(char);
}

indexIfArr ([1, 3,4, 5] ,5 )